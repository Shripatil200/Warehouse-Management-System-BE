# DATABASE - Local defaults for testing
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/wms}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:0000}

# MAIL - Local defaults
spring.mail.username=${SMTP_USER:test@example.com}
spring.mail.password=${SMTP_PASS:password}

# APP CUSTOM - Fixes the APP_CORS_ALLOWED_ORIGINS error
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}
app.frontend.url=${APP_FRONTEND_URL:http://localhost:3000}
app.sms.api.key=${SMS_API_KEY:test-key}

# Keep Redis pointing to local for tests
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}